#include <stdlib.h>
#include "main.h"

/**
 * _abs - prints absolute value of an integer
 * @r: integer
 * Return: absolute value of integer
 */
int _abs(int r)
{
	abs(r);
	_putchar (r);
	return (0);
}
