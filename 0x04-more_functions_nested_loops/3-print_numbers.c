#include "main.h"

/**
 * print_numbers - prints numbers from 0 to 9
 * followed by a newline
 */

void print_numbers(void)
{
	int q;

	for (q = 0; q < 10; q++)
	{
		_putchar(q);
	}
	_putchar('\n');
}
